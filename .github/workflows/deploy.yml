name: Bxb的文档笔记

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 检出代码到打包环境中
        uses: actions/checkout@v4

      - name: 安装Node
        uses: actions/setup-node@v3
        with:
          node-version: 20
          cache: 'yarn'

      - name: 启用Github Pages并读取文档元数据
        uses: actions/configure-pages@v4

      - name: 安装依赖
        run: yarn install --frozen-lockfile

      - name: 打包
        run: yarn build

      - name: 上传项目
        uses: actions/upload-pages-artifact@v2  # 使用正确的action名称
        with:
          path: docs/.vitepress/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: 部署到 GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v2
